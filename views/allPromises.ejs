<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Promises - Promise Tracker</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #eee;
    }
    .navbar {
      background: #333;
      color: white;
      padding: 10px;
    }
    .navbar h1 {
      font-size: 18px;
      display: inline;
    }
    .navbar-right {
      display: inline;
      margin-left: 15px;
    }
    .navbar a {
      color: white;
      margin-left: 15px;
      text-decoration: none;
    }
    .container {
      max-width: 1200px;
      margin: 10px auto;
      padding: 10px;
    }
    h2 {
      margin-bottom: 10px;
      color: #333;
    }
    .promise-card {
      background: #f9f9f9;
      padding: 15px;
      margin-bottom: 10px;
      border: 1px solid #ccc;
    }
    .promise-title {
      font-weight: bold;
      color: #333;
      margin-bottom: 5px;
    }
    .politician-info {
      color: #666;
      font-size: 14px;
      margin-bottom: 5px;
    }
    .promise-description {
      color: #333;
      line-height: 1.4;
      margin: 10px 0;
    }
    .promise-footer {
      margin-top: 10px;
      padding-top: 10px;
      border-top: 1px solid #ccc;
    }
    .status-badge {
      padding: 5px 10px;
      font-size: 13px;
      border: 1px solid #999;
      display: inline-block;
    }
    .status-ยังไม่เริ่ม {
      background: #fff3cd;
      color: #856404;
    }
    .status-กำลังดำเนินการ {
      background: #d1ecf1;
      color: #0c5460;
    }
    .status-เงียบหาย {
      background: #f8d7da;
      color: #721c24;
    }
    .date {
      color: #999;
      font-size: 13px;
      display: inline-block;
      margin-right: 10px;
    }
    .btn {
      display: inline-block;
      padding: 8px 15px;
      background: #0066cc;
      color: white;
      text-decoration: none;
      border: none;
    }
    .empty-state {
      text-align: center;
      padding: 60px 10px;
      color: #999;
    }
  </style>
</head>
<body>
  <div class="navbar">
    <h1>Politician Promise Tracker</h1>
    <div class="navbar-right">
      <span>User: <%= user.username %></span>
      <a href="/promises">All Promises</a>
      <a href="/politicians">Politicians</a>
      <a href="/logout">Logout</a>
    </div>
  </div>

  <div class="container">
    <h2>All Promises (sorted by announcement date)</h2>

    <% if (promises.length === 0) { %>
      <div class="empty-state">
        <p>No promises in system</p>
      </div>
    <% } else { %>
      <% promises.forEach(promise => { %>
        <div class="promise-card">
          <div class="promise-header">
            <div>
              <div class="promise-title">Promise #<%= promise.id %></div>
              <div class="politician-info">
                <%= promise.politicianName %> (<%= promise.politicianParty %>)
              </div>
            </div>
            <span class="status-badge status-<%= promise.status %>">
              <%= promise.status %>
            </span>
          </div>
          
          <div class="promise-description">
            <%= promise.description %>
          </div>
          
          <div class="promise-footer">
            <span class="date">Announced: <%= promise.announcedDate %></span>
            <a href="/promises/<%= promise.id %>" class="btn">View Details</a>
          </div>
        </div>
      <% }); %>
    <% } %>
  </div>
</body>
</html>
