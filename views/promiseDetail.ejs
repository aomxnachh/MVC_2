<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Promise Details - Promise Tracker</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #eee;
    }
    .navbar {
      background: #333;
      color: white;
      padding: 10px;
    }
    .navbar h1 {
      font-size: 18px;
      display: inline;
    }
    .navbar-right {
      display: inline;
      margin-left: 15px;
    }
    .navbar a {
      color: white;
      margin-left: 15px;
      text-decoration: none;
    }
    .container {
      max-width: 900px;
      margin: 10px auto;
      padding: 10px;
    }
    .back-link {
      display: inline-block;
      margin-bottom: 10px;
      color: #0066cc;
      text-decoration: none;
    }
    .promise-detail {
      background: #f9f9f9;
      padding: 15px;
      border: 1px solid #ccc;
      margin-bottom: 15px;
    }
    .detail-header {
      margin-bottom: 10px;
      padding-bottom: 10px;
      border-bottom: 1px solid #ccc;
    }
    .detail-title {
      font-size: 20px;
      color: #333;
      margin-bottom: 5px;
    }
    .politician-info {
      color: #666;
      margin-bottom: 5px;
    }
    .status-badge {
      padding: 5px 10px;
      border: 1px solid #999;
      font-size: 13px;
      display: inline-block;
      margin-top: 5px;
    }
    .status-ยังไม่เริ่ม {
      background: #fff3cd;
      color: #856404;
    }
    .status-กำลังดำเนินการ {
      background: #d1ecf1;
      color: #0c5460;
    }
    .status-เงียบหาย {
      background: #f8d7da;
      color: #721c24;
    }
    .promise-description {
      font-size: 14px;
      line-height: 1.4;
      color: #333;
      margin-bottom: 10px;
    }
    .promise-meta {
      color: #999;
      font-size: 13px;
      margin-bottom: 10px;
    }
    .btn {
      padding: 8px 15px;
      text-decoration: none;
      display: inline-block;
      margin-right: 5px;
      margin-top: 5px;
    }
    .btn-primary {
      background: #0066cc;
      color: white;
      border: none;
    }
    .btn-secondary {
      background: #666;
      color: white;
      border: none;
    }
    .updates-section {
      background: #f9f9f9;
      padding: 15px;
      border: 1px solid #ccc;
    }
    .updates-title {
      font-size: 18px;
      color: #333;
      margin-bottom: 10px;
      padding-bottom: 5px;
      border-bottom: 1px solid #ccc;
    }
    .update-item {
      padding: 10px;
      margin-bottom: 10px;
      background: white;
      border: 1px solid #ccc;
      border-left: 3px solid #333;
    }
    .update-date {
      color: #333;
      font-weight: bold;
      margin-bottom: 5px;
    }
    .update-description {
      color: #333;
      line-height: 1.4;
    }
    .empty-updates {
      text-align: center;
      padding: 30px;
      color: #999;
    }
  </style>
</head>
<body>
  <div class="navbar">
    <h1>Politician Promise Tracker</h1>
    <div class="navbar-right">
      <span>User: <%= user.username %></span>
      <a href="/promises">All Promises</a>
      <a href="/politicians">Politicians</a>
      <a href="/logout">Logout</a>
    </div>
  </div>

  <div class="container">
    <a href="/promises" class="back-link">← Back to All Promises</a>

    <div class="promise-detail">
      <div class="detail-header">
        <div>
          <div class="detail-title">Promise #<%= promise.id %></div>
          <div class="politician-info">
            Politician: <strong><%= politician ? politician.name : 'ไม่ทราบชื่อ' %></strong>
          </div>
          <div class="politician-info">
            Party: <strong><%= politician ? politician.party : 'ไม่ทราบพรรค' %></strong>
          </div>
        </div>
        <span class="status-badge status-<%= promise.status %>">
          <%= promise.status %>
        </span>
      </div>

      <div class="promise-description">
        <%= promise.description %>
      </div>

      <div class="promise-meta">
        Announced: <%= promise.announcedDate %>
      </div>

      <div class="action-buttons">
        <% if (promise.status !== 'เงียบหาย' && canUpdate) { %>
          <a href="/promises/<%= promise.id %>/update" class="btn btn-primary">
            Update Progress
          </a>
        <% } %>
        <a href="/politicians/<%= promise.politicianId %>" class="btn btn-secondary">
          View Other Promises by this Politician
        </a>
      </div>
    </div>

    <div class="updates-section">
      <h3 class="updates-title">Update History</h3>
      
      <% if (updates.length === 0) { %>
        <div class="empty-updates">
          No updates yet
        </div>
      <% } else { %>
        <% updates.forEach(update => { %>
          <div class="update-item">
            <div class="update-date">
              <%= update.updateDate %>
            </div>
            <div class="update-description">
              <%= update.description %>
            </div>
          </div>
        <% }); %>
      <% } %>
    </div>
  </div>
</body>
</html>
